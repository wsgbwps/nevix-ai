构建时需要注入 `__NEVIX_SUPABASE_URL__`、`__NEVIX_SUPABASE_PUBLISHABLE_KEY__`、`__NEVIX_SUPABASE_CONFIG_POLICY__` 三个编译期常量，由 `readSupabasePublicConfig` 解析为 `SupabasePublicConfig`；主进程需实现 `authentication:read-session`、`authentication:clear-session`、`authentication:replace-session` 三个 IPC 通道。