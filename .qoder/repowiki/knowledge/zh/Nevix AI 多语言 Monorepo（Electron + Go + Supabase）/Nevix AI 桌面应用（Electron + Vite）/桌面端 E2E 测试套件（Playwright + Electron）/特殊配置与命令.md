认证类测试需要预先构建特定产物：`configuration.spec.ts` 要求 `NEVIX_EXPECT_INVALID_SUPABASE_CONFIG=1` 的失败构建，`login-boundary.spec.ts` 等需要 `NEVIX_TEST_SUPABASE_URL`/`NEVIX_TEST_SUPABASE_PUBLISHABLE_KEY`/`NEVIX_TEST_SUPABASE_SERVICE_ROLE_KEY` 指向可访问的 Supabase 实例；i18n 测试依赖 `../../build/locales` 下的 macOS 本地化文件与 `electron-builder.yml` 中的 `installerLanguages` 配置。