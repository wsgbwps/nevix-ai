E2E 测试通过环境变量控制行为：`NEVIX_E2E=1` 启用测试模式，`NEVIX_TEST_USER_DATA_DIR` 指定用户数据目录，`NEVIX_TEST_FORCE_BASIC_TEXT_STORAGE=1` 强制明文存储以便调试，`NEVIX_TEST_UNAVAILABLE_SECURE_STORAGE=1` 模拟安全存储不可用，`NEVIX_TEST_SYSTEM_LANGUAGES` 覆盖系统语言列表。开发时可通过 `ELECTRON_RENDERER_URL` 直接加载渲染端地址。